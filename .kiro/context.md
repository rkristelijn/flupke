# flupke agent context
This is a monorepo with 100+ npm packages under packages/.
Each package is a zero-dependency drop-in replacement for a neglected npm package.
Run tests: node --test packages/*/test/*.test.js

